{
  "name": "<%= projectName %>",
  "description": "This project was generated from the Mintbean vanilla-js template using the mintbean-cli tool.",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "mintbean": {
    "template": "<%= templateName %>",
    "scripts": {
      "predeploy": "parcel build index.html --public-url https://<%= githubUsername %>.github.io/<%= projectName %>/",
      "deploy": "gh-pages -d dist"
    }
  },
  "devDependencies": {
    "parcel": "^1.12.4",
    "gh-pages": "^3.1.0"
  }
}
